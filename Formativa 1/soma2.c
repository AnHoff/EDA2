#include <stdio.h>

int main() {
    int num1, num2;
    
    // Lê os dois números da entrada padrão
    scanf("%d %d", &num1, &num2);
    
    // Calcula a soma dos dois números
    int soma = num1 + num2;
    
    // Imprime a soma
    printf("%d\n", soma);
    
    return 0;
}
